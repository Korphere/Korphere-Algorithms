export default function heap_sort(target: number[]): number[] {
    /* No Implementation Now */
    return target;
}

export function heap_sort_mut(target: number[]): number[] {
    /* No Implementation Now */
    return target;
}