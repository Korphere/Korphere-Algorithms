export default function quick_sort(target: number[]): number[] {
    /* No Implementation Now */
    return target;
}

export function quick_sort_mut(target: number[]): number[] {
    /* No Implementation Now */
    return target;
}